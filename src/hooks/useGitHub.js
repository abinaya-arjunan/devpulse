// useGitHub.js — custom hook stub
// Day 2: full fetch logic goes here
// For now just exports the shape so imports don't break

function useGitHub(username) {
  // Will return: { user, repos, loading, error }
  return { user: null, repos: [], loading: false, error: null };
}

export default useGitHub;